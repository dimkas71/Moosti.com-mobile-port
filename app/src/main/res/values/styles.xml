<resources>

    <!-- Base application theme. -->

    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.Menu" parent="Theme.AppCompat.Light">
        <item name="android:background">@color/accent_material_light</item>
        <item name="android:textColorPrimary">@color/primary_material_light</item>
        <item name="android:selectedWeekBackgroundColor">@color/accent_material_light</item>
    </style>

</resources>
